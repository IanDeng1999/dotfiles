---
description: App Idea To Json-Server Schema
---

我想做一个APP，基本信息在这里，我想用json server作为后端服务，你给我分析一下数据库应该怎么设计，应该有哪些表：

$@

## 交付规范

- 输出 `db.json` 文件，要求每个表必须都是空数组，只有字段定义，没有数据
- 用纯文本（没有表格）输出表之间的用途和关系到 `entity-design.md`
